/*import React from "react"

export default class CheckBox extends React.Component{
      state = {
        isChecked: false
      };
  
    handleChecked = () => this.setState({isChecked: !this.state.isChecked});

    render(){
      return <label><input type="checkbox" onChange={ this.handleChecked }/> O ToDo {this.state.isChecked ? 'foi' : 'nao foi'} realizado</label>
    }
  }
  */
